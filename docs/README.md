# docs — design doc index

Design-doc-first: each service ships `docs/<name>-design.md` before implementation (mirrors `services/<name>/`).

| Service            | Design doc                       | Status                                       |
| ------------------ | -------------------------------- | -------------------------------------------- |
| gogs (private git) | [gogs-design.md](gogs-design.md) | operational (macOS + Linux, verified by dev) |
